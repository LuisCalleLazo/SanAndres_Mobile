import 'package:flutter/material.dart';

class DataAutopartsPage extends StatelessWidget {
  static String name = "data_autoparts_page";
  const DataAutopartsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Gestion de autopartes"),
      ),
      body: Container(),
    );
  }
}