import 'package:flutter/material.dart';

class AddClientPage extends StatelessWidget {
  static String name = "add_client_page";
  const AddClientPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
