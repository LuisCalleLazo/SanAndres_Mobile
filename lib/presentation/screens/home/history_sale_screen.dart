import 'package:flutter/material.dart';

class HistorySaleScreen extends StatelessWidget {
  const HistorySaleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("HISTORIAL DE VENTAS"),
    );
  }
}
